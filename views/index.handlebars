<h1>Burger Eater Inc.</h1>

<h2>Eat a burger, Make a burger, or add your own custom burger</h2>

<h3>Eat a burger</h3>
<ul>
    {{#each burgers}}
        {{#unless eaten}}
            {{burger_name}}
                <button class="eat-burger" data-id="{{id}}">
                    EAT!
                </button>
        {{/unless}}
    {{/each}}
</ul>

<h3>Make a burger</h3>
<ul>
    {{#each burgers}}
        {{#if eaten}}
            {{burger_name}}
                <button class="make-burger" data-id="{{id}}">
                    MAKE!
                </button>
        {{/if}}
    {{/each}}
</ul>
<h3>Create a burger</h3>
<form class="create-form">

  <div class="form-group">
    <label for="burger-name">Burger Name:</label>
    <input type="text" id="burger-name" name="name">
  </div>

  <button type="submit">Create Burger</button>
</form>